apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: metabase-data
  namespace: rest
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path      # K3s default storage class
  resources:
    requests:
      storage: 2Gi
