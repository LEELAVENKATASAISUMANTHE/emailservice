# This secret is created/updated by CI/CD (GitHub Actions) from repository secrets.
# Do NOT apply this file manually — it has no values.
# To create it locally for testing, run:
#
#   kubectl create secret generic backend-secret \
#     --from-literal=DATABASE_URL="..." \
#     --from-literal=ZEPTOMAIL_TOKEN="..." \
#     --from-literal=FROM_EMAIL="..." \
#     --from-literal=OBJECT_STORAGE_ENDPOINT="..." \
#     --from-literal=OBJECT_STORAGE_ACCESS_KEY="..." \
#     --from-literal=OBJECT_STORAGE_SECRET_KEY="..." \
#     --namespace rest \
#     --dry-run=client -o yaml | kubectl apply -f -
